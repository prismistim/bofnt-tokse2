import type { Config } from 'tailwindcss'

export default <Partial<Config>>{
  darkMode: 'class',
  theme: {
    fontFamily: {
      sans: [
        'heebo',
        'ryo-gothic-plusn',
        'sans-serif',
      ],
    },
  },
}
